package org.learn;

import org.learn.client.RpcClient;
import org.learn.server.EchoService;

public class RpcClientTest {
    public static void main(String [] args){
        RpcClient<EchoService> rpcClient = new RpcClient<EchoService>();
        EchoService echoService = rpcClient.e
    }
}
